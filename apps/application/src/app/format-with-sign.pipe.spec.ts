import {FormatWithSignPipe} from './format-with-sign.pipe';

describe('FormatWithSignPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWithSignPipe();
    expect(pipe).toBeTruthy();
  });
});
